function calculate() {
    var inputVariable = document.getElementById('inputVariable').value;
    var result = inputVariable * 2; // Możesz tutaj wprowadzić dowolne obliczenia
    document.getElementById('result').innerText = "Wynik: " + result;
}